@model IEnumerable<waTallerJhaka.Models.Vehiculo>

@{
    ViewData["Title"] = "listadoVehiculos";
    Layout = "~/Views/Shared/LayoutPrincipal.cshtml";
}
<hr />
<div class="tables-wrapper">
    <div class="row">
        <div class="col-lg-12">
            <div class="card-style mb-30">
                <h6 class="mb-10">Lista de Vehiculos</h6>
                <div class="table-wrapper table-responsive">
                    <table id="tabla" class="table">
                            <thead>
                                <tr>
                                    <th>
                                        @Html.DisplayNameFor(model => model.ide_veh)
                                    </th>
                                    <th>
                                        @Html.DisplayNameFor(model => model.mar_veh)
                                    </th>
                                    <th>
                                        @Html.DisplayNameFor(model => model.mod_veh)
                                    </th>
                                    <th>
                                        @Html.DisplayNameFor(model => model.pla_veh)
                                    </th>
                                    <th>
                                        @Html.DisplayNameFor(model => model.nom_cli)
                                    </th>
                                    <th></th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.ide_veh)
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.mar_veh)
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.mod_veh)
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.pla_veh)
                                        </td>
                                        <td>
                                           <a href="#">@Html.DisplayFor(modelItem => item.nom_cli)</a> 
                                        </td>
                                        <td>
                                            @Html.ActionLink("Editar", "editarVehiculo", new { id = item.ide_veh }) |
                                            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                </div>
                <div class="d-flex justify-content-between">
                    <select id="rowsPerPage" class="form-select w-auto mb-3">
                        <option value="5">5 por página</option>
                        <option value="10">10 por página</option>
                        <option value="20">20 por página</option>
                    </select>
                    <div id="pagination" class="d-flex justify-content-center mt-3"></div>
                </div>
            </div>
        </div>
    </div>
</div>